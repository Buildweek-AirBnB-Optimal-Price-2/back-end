const verify_token = require("./verify_token");
const verify_registration = require("./verify_registration");
const verify_login = require("./verify_login");
const test_verification = require("./test_verification");

module.exports = {
  verify_token,
  verify_registration,
  verify_login,
  test_verification
};